import { KeyEnvelope } from "@hydra-manager/common/Types";

const generateRandomEdd2519PrivateKey = (type: string) => {
  const key: KeyEnvelope = {
    type: type,
    description: "",
    cborHex: "",
  };
};

// lalala
